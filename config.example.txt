# Copy this file to config.txt and fill values locally.
# config.txt is ignored by git. Never commit credentials to this public repository.

# SSH target
SSH_HOST=203.0.113.10
SSH_PORT=22
SSH_USER=ubuntu

# SSH authentication mode: key or password.
# For key mode, set SSH_PRIVATE_KEY_PATH and leave SSH_PASSWORD empty.
# For password mode, set SSH_PASSWORD and leave SSH_PRIVATE_KEY_PATH empty.
# Password mode is acceptable for this single-server Demo; scripts must pass it via
# environment/stdin or a library API, never as a visible command-line argument.
SSH_AUTH_MODE=password
SSH_PRIVATE_KEY_PATH=
SSH_PASSWORD=
SSH_BASTION=

# Project directory on the remote host
REMOTE_WORKDIR=/opt/agentnest-demo
REMOTE_TIMEZONE=UTC

# OpenClaw stable version policy
OPENCLAW_CHANNEL=stable
# AUTO resolves the newest official stable release at deployment time.
OPENCLAW_VERSION=AUTO
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BIND_HOST=127.0.0.1

# Model provider supported by the selected OpenClaw stable version
MODEL_PROVIDER=openai
MODEL_NAME=
MODEL_API_KEY=

# Demo services
CONTROL_PLANE_PORT=18080
DATA_GATEWAY_MOCK_PORT=18081
EXTERNAL_GATEWAY_MOCK_PORT=18082
POSTGRES_PORT=15432
POSTGRES_PASSWORD=

# Optional static token for the external Demo API.
# Leave empty when the API is only reached through an SSH tunnel.
DEMO_API_TOKEN=

# Lifecycle defaults. Tests use a fake clock.
L1_IDLE_TTL_SECONDS=86400
L2_IDLE_TTL_SECONDS=3600

# Keep false for the default SSH-tunnel deployment.
EXPOSE_DEMO_API=false